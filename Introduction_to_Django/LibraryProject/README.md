# LibraryProject

This is a basic Django project for a library system.

## Setup

1. Clone the repository.
2. Navigate to `Introduction_to_Django/LibraryProject`.
3. Install dependencies: `pip install -r requirements.txt` (after generating it).
4. Run migrations: `python manage.py migrate`
5. Run the development server: `python manage.py runserver`
